#include<stdio.h>
/**
 * Main - A program that uses the printf function to print text
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
